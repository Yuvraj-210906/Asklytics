# Asklytics
Asklytics uses Python, FastAPI, Pandas, Plotly, and AI to analyze structured datasets and unstructured documents. It dynamically generates tailored analysis code for each request instead of relying only on fixed reports. A self-healing mechanism detects errors, refines the generated code, and retries automatically to improve reliability.
